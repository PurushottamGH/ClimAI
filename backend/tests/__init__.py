"""Focused unit tests for the ClimAI backend.

Run from the repository root:

    python -m unittest discover -s backend/tests -v

Runtime dependencies are the packages already listed in backend/requirements.txt;
the tests use only the standard library test runner.
"""
